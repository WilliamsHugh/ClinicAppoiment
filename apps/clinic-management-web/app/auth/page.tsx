import { PagePlaceholder } from "../../src/components/page-placeholder";

export default function AuthPage() {
  return <PagePlaceholder eyebrow="Authentication" title="Đăng nhập" description="Thành viên phụ trách xác thực sẽ gắn form và adapter phiên tại đây." />;
}
